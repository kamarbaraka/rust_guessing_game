[package]
name = "guessing_game"
version = "0.1.0"
edition = "2024"
repository = "https://github.com/kamarbaraka/rust_guessing_game.git"

[dependencies]
rand = "0.8.5"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
